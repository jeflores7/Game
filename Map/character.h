class character {
	public:
		//int TakeTurn(); //the character uses its turn. It decides to move, interact, wait, etc. 
							 // Returns 1 if successful, 0 otherwise
		//int TakeDamage();
		//int ChangeHealth();
	private:
		int health;
		int attack;
		int defense;
};
